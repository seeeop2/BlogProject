<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xmlns:tx="http://www.springframework.org/schema/tx"
xmlns:aop="http://www.springframework.org/schema/aop"
xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/aop
http://www.springframework.org/schema/aop/spring-aop.xsd
http://www.springframework.org/schema/tx
http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- Transaction 설정 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- Transaction을 위한 AOP 설정 -->
    <aop:config proxy-target-class="true">
        <aop:pointcut id="servicePublicMethod" expression="execution(public * com.inseop.web.board..*(int))" />
        <!--ㄴ공통기능을 적용 할 대상을 지정-->
        <!-- public   *       com.inseop.web.board..      *        (int)
                    반환형     패키지 경로 및 클래스명      메스드명     파라미터
            com.inseop.web.board 패키지 하위에 포함되어 있는 모든 패키지 중에서 반환형은 상관없이 모두 대상.
            그 중 파라미터 타입이 int인 인자를 하나만 가지고 있는 클래스가 대상
            아래의 클래스들이 대상이 되는 클래스
            -deleteBoard(int)
            -getBoardContent(int)
            -updateViewCnt(int)
        -->
        <aop:advisor advice-ref="txAdvice" pointcut-ref="servicePublicMethod" />
        <!--aop:advice는 공통기능을 가지고 있는 구현체-->
    </aop:config>

    <!-- 선언적 Transaction 설정  -->
    <tx:advice id="txAdvice" transaction-manager="transactionManager">
        <tx:attributes>
            <tx:method name="getBoardContent" rollback-for="Exception" />
            <!--속성으로 적용할 메소드 이름을 적어 주고, 트랜잭션 발생 시 어떠한 동작을 할 지 지정
                "getBoardContent메소드에서 exception이 발생하면 롤백"

            -->
        </tx:attributes>
    </tx:advice>


    <tx:annotation-driven transaction-manager="transactionManager" proxy-target-class="true"/>

</beans>